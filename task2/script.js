document.querySelector("#consoleLog").addEventListener("click", () => {
  alert(
    "это метод в JavaScript, который позволяет выводить любые сообщения прямо в консоль браузера. Эта возможность необходима при отладке и тестировании кода."
  );
});
document.querySelector("#alert").addEventListener("click", () => {
  alert(
    "Функция alert() в JavaScript выводит в браузере предупреждающее модальное диалоговое окно с сообщением и кнопкой «ОК»"
  );
});
document.querySelector("#prompt").addEventListener("click", () => {
  alert(
    "prompt() — это функция в JavaScript, которая используется для получения данных от пользователя на веб-странице."
  );
});
